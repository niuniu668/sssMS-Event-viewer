# 26.4.23
需要设置两层目录树，方便查看
设置一个勾选的选项，当选中这个文件夹时，首先显示堆叠图，设置堆叠图的波形数量最多为10个，超过10个就不显示堆叠图了
设置一个勾选的选项，是否显示频谱图，当选中一个波形时，可以在波形图的下方生成一个频谱图，显示这个波形的频谱图，这波形的频谱图实时随着波形的放大而改变计算范围，需要规定一下，当前软件的波形采样率均为4000HZ
设置一个勾选的选项，是否显示时频图，当选中一个波形时，可以在波形图的下方生成一个时频图，显示这个波形的时频图，这波形的时频图实时随着波形的放大而改变计算范围和显示范围，应该是根据波形的横坐标范围而变化。
如果频谱和时频图都显示，那就是选中一个波形情况下显示三个图了，分别是波形图、频谱图和时频图
波形图上的 sample=  和AMP=  的数值显示在波形图的右上角
波形图、频谱图和时频图的刻度需要设置好

#26.4.24
现在这个版本我需要进行升级改进，需要创建一个新的分支。需要升级的是需要增加一个模块，这个模块的功能是对找全波形。我有些波形数据没有在EVent文件夹，需要到data文件夹里去寻找。比如现在的波形数据在E:\GMS工程版V2.0.7.270\Event\1285微震\2026\04\07\2026.04.07.02.27.40.106-5这个文件夹里，我的全部波形就在E:\GMS工程版V2.0.7.270\Data\1285微震\1\2026\04\07\2 类似的文件夹中，我来解释一下这个data文件夹地址的规则，Data就是存放data数据的文件夹，1285微震是这个项目的名字，1是传感器编号（对应event二进制波形数据的名称），2026\04\07\2是data数据的日期和时间（小时），data波形数据的名称是分钟，所以每个data数据是一分钟的波形数据，注意不是二进制储存，可以用转化为csv文件读取（你可以看看怎么读取）。Event数据的文件夹名称2026.04.07.02.27.40.106-5就代表了事件的日期时间，所以要通过这个事件时间来找到所有的data数据，找到这个事件时间所在的分钟，然后把这个分钟的data数据加载到界面上，这样就可以在界面上看到这个事件的波形了。这个模块的功能就是要把这个事件的波形找全了，找全了之后就可以进行后续的分析了。需要设置一个data文件夹地址的输入框。然后在第一选项卡（就是显示波形的功能）的第一个目录列表（文件夹列表）的文件夹选中后，设置一个勾选的寻找data数据的选项（这个选项的优先级为当前所有勾选选项之前），当选中这个选项时，就会根据这个事件的时间来寻找data数据，找到这个事件时间所在的分钟，然后把这个分钟的data数据加载到界面上，这样就可以在界面上看到这个事件的波形了。需要设置一个勾选的选项，当选中这个选项时，就会把这个事件的波形找全了，找全了之后就可以进行后续的分析了。

当前这个软件版本需要改进升级，主要是将find corresponding data 的按钮弄成一个单独的页面选项，这个软件的第一个页面就是只对event波形查看，第二个页面才是data文件查看。以及目前的寻找data数据没有一个逻辑（就是需要需要data文件地址中的传感器名称，这个名称是要读取event文件夹中的文件名的），然后就是需要读取的是event发生时间点的波，所以需要对一分钟的波形进行截取，发生时间点前1000个点和后3000个点，共4000个点。这个逻辑能明白吗

还是不行，我打开选中Event波形的文件夹，但是没有加载出来data数据以及波形

需要修改，在data波形展示的界面，首先要修改逻辑，就是现在event波形有几个就会去data文件夹里寻找几个，但是有些传感器的data数据没有在event中显示出来，所以需要在data传感器名称的文件夹列表里把其他传感器的data波形也找到并显示。然后就是绘图了，需要确定一个EVENT时间点，然后在所有的data波形中截取，并堆叠图也展示的也是这4000个点，而不是现在全部一分钟的数据，可以增加一个按钮，显示全部波形时，再可以打开每个data文件的一分钟数据。然后就是绘图的时间刻度线需要优化一下，4000个点，怎么绘制才好，纵坐标刻度线也可以优化一下。




#26.4.25
现在这个版本我需要进行升级改进，需要创建一个新的分支。需要升级的是需要增加一个模块，这个模块与现有的ui界面平行，相当于两个选项卡。这个模块的功能是要对波形进行p波和s波到时拾取，需要设置各种常见的滤波功能，以及到时拾取算法，以及人工到时拾取。

# 第三版发布说明
- 建议版本号：v3.0.0
- 发布名称：Qt Waveform Viewer 第三版
- 发布日期：2026-04-25

## 这次版本的主要内容
- 将界面整理为两个选项卡，第一页专注 Event 波形查看，第二页专注 Data 文件查看。
- 补全 Event 到 Data 的查找逻辑，按事件时间和传感器目录定位对应的 data 文件。
- Data 页支持显示找到的 data 文件完整路径，并优先展示 Event 文件夹列表。
- 事件波形与 data 波形支持按事件时间截取 4000 点窗口，用于堆叠展示与后续分析。
- 已完成 Release 打包，生成可分发的 dist 包。

## 发布建议
- 如果后续继续沿用同一套发布节奏，后续版本可继续采用 v3.x.x 的语义化版本号。
- 如果要在 GitHub 上做正式 Release，建议使用标签名 v3.0.0，对应本次 dist 产物。